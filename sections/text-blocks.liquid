<div class="container-fluid">
    <div class="container">
        <div class="text-block">
            <div class="text-block-head">
                <h2 class="m-h4">{{section.settings.title}}</p>
            </div>
            <div class="text-blocks">
                {% for block in section.blocks %}
                <div class="text-block-item">
                    <p class="faq-question">{{block.settings.title}}</p>
                    <div class="text-block-text">
                        <p class="b-16-r">{{block.settings.text}}</p>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</div>
{% schema %}
    {
    "name": "Text Blocks",
    "settings": [
      {
        "type": "header",
        "content": "Text"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      }
    ],
    "blocks": [
    {
      "type": "text",
      "name": "Text Blocks",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Title"
        },
        {
            "type":"richtext",
            "id": "text",
            "label": "Text",
            "default": "<p>Text</p>" 
        }
      ]
    }
  ],
  "presets": [
    {
      "category": "Text Blocks",
      "name": "text",
      "settings": {},
      "blocks": [
        {
          "type": "text",
          "settings": {}
        },
        {
          "type": "text",
          "settings": {}
        },
        {
          "type": "text",
          "settings": {}
        },
        {
          "type": "text",
          "settings": {}
        }
      ]
    }
  ]
}
{% endschema %}
  